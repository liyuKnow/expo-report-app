import React from "react";
import { Text, View } from "react-native";

const CreateTables = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
        padding: 20,
      }}
    >
      <Text style={{ color: "pink", fontSize: 24 }}>Tables to be created!</Text>
    </View>
  );
};

export default CreateTables;
